name: our_story
description: A new Flutter project.

environment:
  sdk: '>=3.0.1 <4.0.0'


dependencies:
  flutter:
    sdk: flutter
  go_router:
  flutter_riverpod:
  cupertino_icons: ^1.0.2
  firebase_core: ^2.14.0
  oauth2:
  cloud_firestore:
  table_calendar:
  intl:
  flutter_chat_ui:
  firebase_auth:
  google_sign_in:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0


flutter:
  uses-material-design: true
  assets:
  - assets/images/

